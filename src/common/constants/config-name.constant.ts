export const ConfigName = {
  APP: 'app-config',
  DB: 'db-config',
  JWT: 'jwt-config',
  THROTTLE: 'throttle-config',
  SMTP: 'smtp-config',
  KEY: 'key-config',
  SENTRY: 'sentry-config',
  REDIS: 'redis-config',
  OAUTH: 'oauth-config',
  AWSS3: 'aws-s3-config',
} as const;
